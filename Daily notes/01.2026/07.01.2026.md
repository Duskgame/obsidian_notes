#dailynote

Daily goal:
- [ ] continue unit 4 path 3
- [ ] kwizz

Progress:
Somehow very low motivation to work on [[Adaptive layout design to screen sizes]]


Problems:
preview not working for largest screen in reply project of unit 4 path 3

	Solution:
		val activity = LocalContext.current as Activity
		Keep composables used in previews free of direct `Activity` casts. If something truly needs an 
		`Activity` (e.g., launching an intent), move that to an event callback and handle it in the real 
		`Activity` or `ViewModel`, not inside the composable.
		For now it was enough to commet out the unused activity in ReplyHomeContent.kt

Questions:

New Notes:

Noteworthy:

Meetings:
Daily meeting