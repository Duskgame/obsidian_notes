#dailynote

Daily goal:
- [ ] Continue Course grid

Progress:
	maybe popup menu is promising to try out

Problems:
	Icon has a painter property
	
	LazyVerticalGrid can be used to create scrollable grid 
	
	Additional buttons to sort
	
	```
	@Composable  
	fun GroupByFirstCharacter(topicList: List<Topic>, modifier: Modifier){  
	    val groupedTopics = topicList.groupBy { stringResource(it.title)[0] }  
	    groupedTopics.forEach {  
	        println("${it.key}: ${it.value}")  
	    }  
	}
	```
	it.title would only point toward the string resource
	

Questions:

Vocabulary:

Noteworthy:

Meetings:
App Refinement