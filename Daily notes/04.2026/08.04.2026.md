#dailynote

## Progress:
Kwizz add/edit quiz & question logic:
- Explored two approaches for testing database logic: [[Instrumented Tests|instrumented tests]] with an [[In-memory Room Database|in-memory Room DB]], and a [[Fake Repository|fake repository]] for running the full app with hardcoded data
- Decided on a dedicated [[ViewModel]] per screen (Option B) for the edit flow
- Created `EditQuizUiState`, `EditQuizViewModel`, `EditQuizViewModelFactory`, and `EditQuizScreen`
- Added `EditQuiz` navigation route to `NavigationRoute` and wired the new `composable` destination into `KwizzNavHost`
- Updated `onEditQuizClicked` callback signature from `() -> Unit` to `(QuizWithQuestions) -> Unit` through `StartScreen` and `QuizButtonList`

Kwizz question sorting algorithm implementation
Designed 3-category sorting: new questions first, then last-wrong (sorted by wrong ratio) interleaved with last-correct (sorted by streak)
Created `QuestionWithProgress` data class pairing `Question` with optional `QuestionProgress`
Added `sortedQuestions: List<QuestionWithProgress>` to `QuizUiState`\n\t- Updated `QuizViewModel`:
`setCurrentQuiz` loads progress from DB via [[Flow.first()]], sorts and interleaves into `sortedQuestions`
`buildSortedQuestionList` — splits into new / wrong / correct categories, sorts wrong by ratio and correct by streak
`interleave` — distributes correct questions evenly among wrong questions by position
`persistAnswerResult` — writes `AnswerHistory` event and upserts `QuestionProgress` after each answer
`restartCurrentQuiz` re-runs sort so updated progress is reflected on next run

## Problems:

## Questions:

## New Notes:
[[Flow.first()]]
[[Fake Repository]]
[[In-memory Room Database]]
[[Instrumented Tests]]
[[ViewModelProvider.Factory]]

## Noteworthy:

## Meetings:

## To Do's:

- [ ] Kwizz
	- [ ] more question types
	- [ ] watch [KotlinConf conference video talk](https://youtu.be/w0kfnydnFWI?t=256).
	- [x] implement database logic
- [ ] Apprenticeship
	- [x] App Team
		- [x] XML to compose rewrite
			- [x] create screens
			- [x] fill in data from viewmodel
			- [x] replace code in ShowLogActivity
			- [x] Rework Viewmodel
			- [x] Pull Request Process 
				- [x] KTLint
	- [ ] Berichtsheft 
		- [ ] **Friday Task**
		- [ ] Nr. 6 Praxisblock 3 (App) 16.2.2026- 10.04.2026
	- [ ] Zeiten Buchen
- [ ] School 
	- [ ] Lernfeld 4
		- [ ] 6. Teilhandlung Ergebnisse Ihrer Arbeit darstellen
	- [ ] WuG Workshop WuG 05.02.2026
		- [ ] Fachgespräch 3. Block
	- [ ] SuK Diskussion Verbot Social Media für Jugendliche unter 16 in Deutschland]]